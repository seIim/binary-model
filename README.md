Binary orbit modelling.

We implement the observational based distributions of binary orbit parameters to effectively model their radial velocity curves. Then, we can marginalize over the distribution parameters and create a distribution of the radial velocities for binary star systems.

Then, we can effectively simulate observation runs of large populations of stars to optimize the observation frequency, time seperation, etc... to detect the maximum number of binaries.
